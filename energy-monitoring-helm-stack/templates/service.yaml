apiVersion: v1
kind: Service
metadata:
  name: {{ include "energy-metrics.fullname" . }}
  labels:
    {{- include "energy-metrics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "energy-metrics.selectorLabels" . | nindent 4 }}
